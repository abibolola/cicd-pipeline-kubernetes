# TEMPLATE ONLY. Copy to 02-secret.yaml, set a real password, and DO NOT commit it.
# 02-secret.yaml is gitignored.
#
# Or create it imperatively, which is what the pipeline does:
#   kubectl create secret generic shortener-secrets \
#     --namespace shortener \
#     --from-literal=REDIS_PASSWORD="$(openssl rand -base64 24)" \
#     --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: shortener-secrets
  namespace: shortener
type: Opaque
stringData:
  REDIS_PASSWORD: "CHANGE_ME"
